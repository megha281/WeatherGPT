{
  "name": "weathergpt",
  "version": "1.0.0",
  "private": true,
  "description": "WeatherGPT — Conversational AI for weather forecasting, alerts and climate information. SIH 2026, SIH26068, Team Binary Brains (KU40).",
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "build:rag": "npm --prefix backend run build:rag",
    "test": "npm --prefix backend test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
